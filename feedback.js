(()=>{const form=document.forms['sitepulse-feedback'];if(!form)return;const status=document.getElementById('feedbackStatus');const encode=data=>new URLSearchParams(data).toString();form.addEventListener('submit',async e=>{e.preventDefault();const btn=form.querySelector('button[type="submit"]');const old=btn.textContent;btn.disabled=true;btn.textContent='Sending... / جاري الإرسال...';if(status){status.textContent='';status.style.display='none'}try{const data=Object.fromEntries(new FormData(form).entries());const res=await fetch('/',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:encode(data)});if(!res.ok)throw new Error('HTTP '+res.status);form.reset();if(status){status.textContent='Feedback sent successfully ✅ / تم إرسال ملاحظتك بنجاح ✅';status.style.display='block'}}catch(err){console.error(err);if(status){status.textContent='Could not send feedback. Please try again. / تعذر إرسال الملاحظة، حاول مرة ثانية.';status.style.display='block'}}finally{btn.disabled=false;btn.textContent=old}})})();
